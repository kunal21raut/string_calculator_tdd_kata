
def add(numbers: str) -> int:
    if numbers == "":
        return 0
    
    if not numbers:
        return 0

def add(numbers: str) -> int:
    if not numbers:
        return 0
    return int(numbers)
